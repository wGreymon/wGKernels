// Placeholder for linear attention.
//
// Planned scope:
// - feature map phi(Q), phi(K)
// - non-causal linear attention: phi(Q) * (phi(K)^T * V)
// - causal linear attention with prefix accumulation
// - normalization denominator handling

